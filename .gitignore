node_modules
.next
out
.env
.env.local
.env.*.local
payload-types.ts
.DS_Store
*.log

# SQLite database file (server data — never commit/overwrite this on deploy)
/data/
*.db
*.db-journal

# Local media uploads (server data — never commit/overwrite this on deploy)
/public/media/
