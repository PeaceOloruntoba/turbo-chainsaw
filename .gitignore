node_modules
.next
out
.env
.env.local
.env.*.local
payload-types.ts
.DS_Store
*.log

# Local file storage used in production (uploads + login-protected PDFs).
# This is server data — never commit it or overwrite it on deploy. In
# production point LOCAL_STORAGE_DIR at a folder OUTSIDE the app folder.
/storage/
